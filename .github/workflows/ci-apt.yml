# Generated by velnor-actions-generator. DO NOT EDIT.
# Cache declaration SHA-256: 4b4d185f0bbf6a5d7eaec68e1912a6c0c0f374991eccc7c91c0f604b7de02cbd
#
# Owner-local reusable CI workflow for the apt class. Consumers call their own
# organization's mirror of this file. It owns the lane jobs, runs identical gate
# commands on either lane through the run-gate building block, and emits the lane
# contract only after every applicable gate genuinely succeeds. It inherits no
# secrets and no environment. The internal composite closure is pinned to the
# immutable block SHA recorded in fleet/block-sha.
name: ci-apt

on:
  workflow_call:
    inputs:
      lane:
        description: "CI lane: github, velnor, or both; empty derives from owner."
        required: false
        type: string
        default: ""
    outputs:
      contract:
        description: "'success' only when every selected lane passed its applicable gates."
        value: ${{ jobs.contract.outputs.contract }}

permissions:
  contents: read

jobs:
  velnor-lane:
    name: velnor lane
    if: ${{ inputs.lane == 'velnor' || inputs.lane == 'both' || (inputs.lane == '' && (github.repository_owner == 'tailrocks' || github.repository_owner == 'ChainArgos')) }}
    # Public unmerged-code events (pull_request, merge_group) route the Velnor lane
    # to GitHub-hosted execution until a live-proven lower-trust Velnor pool exists.
    # This is explicit policy routing, not silent failover.
    runs-on: ${{ (github.event_name == 'pull_request' || github.event_name == 'merge_group') && 'ubuntu-latest' || 'velnor-trusted' }}
    timeout-minutes: 30
    outputs:
      contract: ${{ steps.aggregate.outputs.contract }}
    steps:
      - name: Report Velnor lane routing policy
        if: ${{ github.event_name == 'pull_request' || github.event_name == 'merge_group' }}
        shell: bash
        run: |
          echo 'policy: public unmerged-code routes the Velnor lane to GitHub-hosted execution'
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Restore tools cache
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            .cache/mise
            .local/share/mise/installs
          key: ci-v1/apt/tools/${{ runner.os }}-${{ runner.arch }}/${{ hashFiles('mise.lock', 'mise.toml', 'rust-toolchain.toml') }}/${{ hashFiles('mise.lock', 'mise.toml') }}/install
          restore-keys: |
            ci-v1/apt/tools/${{ runner.os }}-${{ runner.arch }}/${{ hashFiles('mise.lock', 'mise.toml', 'rust-toolchain.toml') }}/${{ hashFiles('mise.lock', 'mise.toml') }}/
      - name: Set up mise toolchain
        uses: jdx/mise-action@dad1bfd3df957f44999b559dd69dc1671cb4e9ea # v4.2.1
      - name: install gate
        uses: tailrocks/velnor-actions/actions/run-gate@3057391f93f3bfc0fe570ee08cfcea9533ea3f92
        with:
          name: install
          command: mise install --locked
      - name: build gate
        uses: tailrocks/velnor-actions/actions/run-gate@3057391f93f3bfc0fe570ee08cfcea9533ea3f92
        with:
          name: build
          command: mise run build
      - name: test gate
        uses: tailrocks/velnor-actions/actions/run-gate@3057391f93f3bfc0fe570ee08cfcea9533ea3f92
        with:
          name: test
          command: mise run test
      - name: lint gate
        uses: tailrocks/velnor-actions/actions/run-gate@3057391f93f3bfc0fe570ee08cfcea9533ea3f92
        with:
          name: lint
          command: mise run lint
      - name: format gate
        uses: tailrocks/velnor-actions/actions/run-gate@3057391f93f3bfc0fe570ee08cfcea9533ea3f92
        with:
          name: format
          command: mise run fmt
      - name: Emit lane contract
        id: aggregate
        uses: tailrocks/velnor-actions/actions/aggregate@3057391f93f3bfc0fe570ee08cfcea9533ea3f92

  github-lane:
    name: github lane
    if: ${{ inputs.lane == 'github' || inputs.lane == 'both' || (inputs.lane == '' && github.repository_owner == 'jackin-project') }}
    runs-on: ubuntu-latest
    timeout-minutes: 30
    outputs:
      contract: ${{ steps.aggregate.outputs.contract }}
    steps:
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Restore tools cache
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            .cache/mise
            .local/share/mise/installs
          key: ci-v1/apt/tools/${{ runner.os }}-${{ runner.arch }}/${{ hashFiles('mise.lock', 'mise.toml', 'rust-toolchain.toml') }}/${{ hashFiles('mise.lock', 'mise.toml') }}/install
          restore-keys: |
            ci-v1/apt/tools/${{ runner.os }}-${{ runner.arch }}/${{ hashFiles('mise.lock', 'mise.toml', 'rust-toolchain.toml') }}/${{ hashFiles('mise.lock', 'mise.toml') }}/
      - name: Set up mise toolchain
        uses: jdx/mise-action@dad1bfd3df957f44999b559dd69dc1671cb4e9ea # v4.2.1
      - name: install gate
        uses: tailrocks/velnor-actions/actions/run-gate@3057391f93f3bfc0fe570ee08cfcea9533ea3f92
        with:
          name: install
          command: mise install --locked
      - name: build gate
        uses: tailrocks/velnor-actions/actions/run-gate@3057391f93f3bfc0fe570ee08cfcea9533ea3f92
        with:
          name: build
          command: mise run build
      - name: test gate
        uses: tailrocks/velnor-actions/actions/run-gate@3057391f93f3bfc0fe570ee08cfcea9533ea3f92
        with:
          name: test
          command: mise run test
      - name: lint gate
        uses: tailrocks/velnor-actions/actions/run-gate@3057391f93f3bfc0fe570ee08cfcea9533ea3f92
        with:
          name: lint
          command: mise run lint
      - name: format gate
        uses: tailrocks/velnor-actions/actions/run-gate@3057391f93f3bfc0fe570ee08cfcea9533ea3f92
        with:
          name: format
          command: mise run fmt
      - name: Emit lane contract
        id: aggregate
        uses: tailrocks/velnor-actions/actions/aggregate@3057391f93f3bfc0fe570ee08cfcea9533ea3f92

  contract:
    name: contract
    needs:
      - velnor-lane
      - github-lane
    if: ${{ always() }}
    runs-on: ubuntu-latest
    timeout-minutes: 10
    outputs:
      contract: ${{ steps.verdict.outputs.contract }}
    steps:
      - name: Aggregate lane verdict
        id: verdict
        env:
          LANE: ${{ inputs.lane != '' && inputs.lane || (github.repository_owner == 'jackin-project' && 'github') || ((github.repository_owner == 'tailrocks' || github.repository_owner == 'ChainArgos') && 'velnor') || 'invalid' }}
          VELNOR_RESULT: ${{ needs.velnor-lane.result }}
          VELNOR_CONTRACT: ${{ needs.velnor-lane.outputs.contract }}
          GITHUB_RESULT: ${{ needs.github-lane.result }}
          GITHUB_CONTRACT: ${{ needs.github-lane.outputs.contract }}
        shell: bash
        run: |
          set -euo pipefail
          # Positive per-lane truth table: emit contract=success ONLY when every selected
          # lane succeeded with contract=success and every non-selected lane is skipped with
          # an empty output. A failed, unavailable, or skipped selected lane is never
          # credited, and one lane never substitutes for the other.
          case "${LANE}" in
            velnor)
              if [ "${VELNOR_RESULT}" != "success" ] || [ "${VELNOR_CONTRACT}" != "success" ]; then
                echo "contract: velnor lane did not pass (result '${VELNOR_RESULT}', contract '${VELNOR_CONTRACT}')" >&2
                exit 1
              fi
              if [ "${GITHUB_RESULT}" != "skipped" ] || [ -n "${GITHUB_CONTRACT}" ]; then
                echo "contract: github lane must be skipped for lane=velnor (result '${GITHUB_RESULT}')" >&2
                exit 1
              fi
              ;;
            github)
              if [ "${GITHUB_RESULT}" != "success" ] || [ "${GITHUB_CONTRACT}" != "success" ]; then
                echo "contract: github lane did not pass (result '${GITHUB_RESULT}', contract '${GITHUB_CONTRACT}')" >&2
                exit 1
              fi
              if [ "${VELNOR_RESULT}" != "skipped" ] || [ -n "${VELNOR_CONTRACT}" ]; then
                echo "contract: velnor lane must be skipped for lane=github (result '${VELNOR_RESULT}')" >&2
                exit 1
              fi
              ;;
            both)
              if [ "${VELNOR_RESULT}" != "success" ] || [ "${VELNOR_CONTRACT}" != "success" ]; then
                echo "contract: velnor lane did not pass (result '${VELNOR_RESULT}', contract '${VELNOR_CONTRACT}')" >&2
                exit 1
              fi
              if [ "${GITHUB_RESULT}" != "success" ] || [ "${GITHUB_CONTRACT}" != "success" ]; then
                echo "contract: github lane did not pass (result '${GITHUB_RESULT}', contract '${GITHUB_CONTRACT}')" >&2
                exit 1
              fi
              ;;
            *)
              echo "contract: unrecognized lane '${LANE}'" >&2
              exit 1
              ;;
          esac
          echo "contract=success" >> "${GITHUB_OUTPUT}"
          echo "contract: lane '${LANE}' passed"
