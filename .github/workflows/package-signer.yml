# Generated by velnor-actions-generator. DO NOT EDIT.
name: Package signer

on:
  workflow_call:
    inputs:
      artifact:
        required: true
        type: string
      source-ref:
        required: true
        type: string

permissions: {}

jobs:
  attest:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      id-token: write
      attestations: write
      contents: read
    steps:
      - name: Admit immutable source
        env:
          ARTIFACT: ${{ inputs.artifact }}
          SOURCE_REF: ${{ inputs.source-ref }}
        run: |
          set -euo pipefail
          [[ "$SOURCE_REF" == refs/tags/v* ]]
          [[ "$ARTIFACT" != */* && "$ARTIFACT" != *..* && -n "$ARTIFACT" ]]
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Bind checksum before attestation
        env:
          ARTIFACT: ${{ inputs.artifact }}
        run: |
          set -euo pipefail
          test -f "$ARTIFACT"
          sha256sum -- "$ARTIFACT" > SHA256SUMS
          sha256sum --check --strict SHA256SUMS
      - uses: actions/attest-build-provenance@43d14bc2b83dec42d39ecae14e916627a18bb661 # v3
        with:
          subject-path: ${{ inputs.artifact }}
