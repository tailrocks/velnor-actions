# Generated by velnor-actions-generator. DO NOT EDIT.
name: Package updater

on:
  workflow_call:
    inputs:
      consumer-repository:
        required: true
        type: string
      signer-digest:
        required: true
        type: string
      branch:
        required: true
        type: string
    secrets:
      app_private_key:
        required: true

permissions: {}

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      attestations: read
      contents: read
    outputs:
      evidence: ${{ steps.normalize.outputs.evidence }}
      artifact: ${{ steps.normalize.outputs.artifact }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Poll and bind latest immutable release
        env:
          CONSUMER_REPOSITORY: ${{ inputs.consumer-repository }}
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          test "$CONSUMER_REPOSITORY" = "$GITHUB_REPOSITORY"
@CONSUMER_POLICY_CASES@
          [[ "$SOURCE_REPOSITORY" =~ ^(jackin-project|tailrocks)/[a-z0-9][a-z0-9._-]*$ ]]
          test -n "$ASSET_PATTERNS"
          mkdir -m 0700 verified
          gh api "repos/$SOURCE_REPOSITORY/releases/latest" > verified/release.json
          jq -e 'type=="object" and (.draft|not) and (.prerelease|not) and (.tag_name|type=="string") and (.assets|type=="array")' verified/release.json >/dev/null
          tag=$(jq -r .tag_name verified/release.json)
          [[ "$tag" =~ ^v[0-9]+\.[0-9]+\.[0-9]+([.-][0-9A-Za-z.-]+)?$ ]]
          gh release download "$tag" -R "$SOURCE_REPOSITORY" -p release-manifest.json -p SHA256SUMS -D verified
          mapfile -t matches < <(jq -r '.assets[].name' verified/release.json | while IFS= read -r name; do
            # shellcheck disable=SC2053 # Policy values are intentional glob patterns.
            while IFS= read -r pattern; do [[ "$name" == $pattern ]] && printf '%s\n' "$name"; done <<<"$ASSET_PATTERNS"
          done | sort -u)
          test "${#matches[@]}" -ge 1
          printf '%s\n' "${matches[@]}" > verified/artifacts.list
          artifact=${matches[0]}
          [[ "$artifact" != */* && "$artifact" != *..* ]]
          gh release download "$tag" -R "$SOURCE_REPOSITORY" -p "$artifact" -D verified
          source_ref="refs/tags/$tag"
          source_digest=$(gh api "repos/$SOURCE_REPOSITORY/git/ref/tags/$tag" --jq .object.sha)
          [[ "$source_digest" =~ ^[0-9a-f]{40}$ ]]
          jq -e --arg repo "$SOURCE_REPOSITORY" --arg ref "$source_ref" --arg commit "$source_digest" --arg artifact "$artifact" '
            keys == ["assets","schema","source_commit","source_ref","source_repository","version"] and
            .schema == "velnor.package-release.v1" and
            .source_repository == $repo and .source_ref == $ref and .source_commit == $commit and
            ([.assets[] | select(.name == $artifact)] | length) == 1 and
            all(.assets[]; keys == ["name","sha256"] and (.sha256 | test("^[0-9a-f]{64}$")))
          ' verified/release-manifest.json >/dev/null
          checksum=$(jq -r --arg artifact "$artifact" '.assets[]|select(.name==$artifact)|.sha256' verified/release-manifest.json)
          manifest_checksum=$(awk -v name="$artifact" '$2==name || $2=="*"name {print $1}' verified/SHA256SUMS)
          test "$checksum" = "$manifest_checksum"
          printf '%s  %s\n' "$checksum" "verified/$artifact" | sha256sum --check --strict
          jq -nS --arg artifact "$artifact" --arg checksum "$checksum" --arg source_ref "$source_ref" --arg source_digest "$source_digest" '{artifact:$artifact,checksum:$checksum,source_ref:$source_ref,source_digest:$source_digest}' > verified/identity.json
      - name: Verify hosted provenance
        env:
          CONSUMER_REPOSITORY: ${{ inputs.consumer-repository }}
          SIGNER_DIGEST: ${{ inputs.signer-digest }}
        run: |
          set -euo pipefail
@CONSUMER_SOURCE_CASES@
          SIGNER_WORKFLOW="$SOURCE_REPOSITORY/.github/workflows/package-sign.yml"
          [[ "$SIGNER_DIGEST" =~ ^[0-9a-f]{40}$ ]]
          artifact=$(jq -r .artifact verified/identity.json)
          source_ref=$(jq -r .source_ref verified/identity.json)
          source_digest=$(jq -r .source_digest verified/identity.json)
          gh attestation verify "verified/$artifact" \
            --repo "$SOURCE_REPOSITORY" \
            --source-ref "$source_ref" \
            --source-digest "$source_digest" \
            --signer-workflow "$SIGNER_WORKFLOW" \
            --signer-digest "$SIGNER_DIGEST" \
            --predicate-type https://slsa.dev/provenance/v1 \
            --deny-self-hosted-runners \
            --format json > verified/verification.json
      - id: normalize
        name: Normalize verified evidence
        run: |
          set -euo pipefail
          evidence=$(jq -cS 'if type == "array" and length > 0 then . else error("empty verification") end' verified/verification.json | base64 -w0)
          artifact=$(jq -r .artifact verified/identity.json)
          echo "evidence=$evidence" >> "$GITHUB_OUTPUT"
          echo "artifact=$artifact" >> "$GITHUB_OUTPUT"
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: verified-package
          path: verified
          if-no-files-found: error
          retention-days: 1

  mutate:
    needs: verify
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: read
    steps:
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: verified-package
          path: verified
      - name: Revalidate evidence before credential mint
        env:
          EVIDENCE: ${{ needs.verify.outputs.evidence }}
          BRANCH: ${{ inputs.branch }}
          ARTIFACT: ${{ needs.verify.outputs.artifact }}
        run: |
          set -euo pipefail
          [[ "$BRANCH" =~ ^package-update/[a-z0-9][a-z0-9._-]*$ ]]
          printf '%s' "$EVIDENCE" | base64 -d | jq -e 'type == "array" and length > 0' >/dev/null
          test "$(jq -r .artifact verified/identity.json)" = "$ARTIFACT"
          checksum=$(jq -r .checksum verified/identity.json)
          printf '%s  %s\n' "$checksum" "verified/$ARTIFACT" | sha256sum --check --strict
      - uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v3
        id: app-token
        with:
          app-id: ${{ vars.PACKAGE_UPDATER_APP_ID }}
          private-key: ${{ secrets.app_private_key }}
          owner: ${{ github.repository_owner }}
          repositories: ${{ github.event.repository.name }}
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          token: ${{ steps.app-token.outputs.token }}
          persist-credentials: true
      - name: Create signed-off update branch and PR
        env:
          GH_TOKEN: ${{ steps.app-token.outputs.token }}
          BRANCH: ${{ inputs.branch }}
          BOT_LOGIN: ${{ vars.PACKAGE_UPDATER_BOT_LOGIN }}
          BOT_EMAIL: ${{ vars.PACKAGE_UPDATER_BOT_EMAIL }}
        run: |
          set -euo pipefail
          test "$BRANCH" != "main"
          git config user.name "$BOT_LOGIN"
          git config user.email "$BOT_EMAIL"
          git switch -C "$BRANCH"
          mise run package-update
          if git diff --quiet; then echo NO_CHANGE_VERIFIED; exit 0; fi
          git add --all
          git commit --signoff -m "chore(package): update verified release"
          git push --force-with-lease origin "$BRANCH"
          gh pr view "$BRANCH" >/dev/null 2>&1 || gh pr create --base main --head "$BRANCH" --title "chore(package): update verified release" --body "Verified consumer-owned package update."
