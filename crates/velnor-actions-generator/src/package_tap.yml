# Generated by velnor-actions-generator. DO NOT EDIT.
name: Package update

on:
  schedule:
    - cron: "17 */6 * * *"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  jackin-project:
    if: ${{ github.repository_owner == 'jackin-project' }}
    uses: jackin-project/velnor-actions/.github/workflows/package-updater.yml@@FLEET_SHA@ # @CALVER@
    with:
      consumer-repository: ${{ github.repository }}
      signer-digest: "@SIGNER_DIGEST@"
      branch: "package-update/verified-stable"
    secrets:
      app_private_key: ${{ secrets.PACKAGE_UPDATER_APP_PRIVATE_KEY }}
  tailrocks:
    if: ${{ github.repository_owner == 'tailrocks' }}
    uses: tailrocks/velnor-actions/.github/workflows/package-updater.yml@@FLEET_SHA@ # @CALVER@
    with:
      consumer-repository: ${{ github.repository }}
      signer-digest: "@SIGNER_DIGEST@"
      branch: "package-update/verified-stable"
    secrets:
      app_private_key: ${{ secrets.PACKAGE_UPDATER_APP_PRIVATE_KEY }}
  ChainArgos:
    if: ${{ github.repository_owner == 'ChainArgos' }}
    uses: ChainArgos/velnor-actions/.github/workflows/package-updater.yml@@FLEET_SHA@ # @CALVER@
    with:
      consumer-repository: ${{ github.repository }}
      signer-digest: "@SIGNER_DIGEST@"
      branch: "package-update/verified-stable"
    secrets:
      app_private_key: ${{ secrets.PACKAGE_UPDATER_APP_PRIVATE_KEY }}
  package-update-required:
    if: ${{ always() }}
    needs: [jackin-project, tailrocks, ChainArgos]
    runs-on: ubuntu-latest
    steps:
      - env:
          OWNER: ${{ github.repository_owner }}
          JACKIN: ${{ needs.jackin-project.result }}
          TAILROCKS: ${{ needs.tailrocks.result }}
          CHAINARGOS: ${{ needs.ChainArgos.result }}
        run: |
          set -euo pipefail
          case "$OWNER" in
            jackin-project) test "$JACKIN" = success; test "$TAILROCKS" = skipped; test "$CHAINARGOS" = skipped ;;
            tailrocks) test "$JACKIN" = skipped; test "$TAILROCKS" = success; test "$CHAINARGOS" = skipped ;;
            ChainArgos) test "$JACKIN" = skipped; test "$TAILROCKS" = skipped; test "$CHAINARGOS" = success ;;
            *) echo "unrecognized owner" >&2; exit 1 ;;
          esac
