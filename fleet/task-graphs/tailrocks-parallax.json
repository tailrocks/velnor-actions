{
  "slug": "tailrocks/parallax",
  "class": "code",
  "scheduled_gates": [
    "install",
    "build",
    "test",
    "lint",
    "format"
  ],
  "tasks": [
    {
      "name": "build",
      "cmd": "cargo build --workspace --all-targets --locked",
      "dir": ".",
      "leaf_identity": "cargo build --workspace --all-targets --locked|dir=.|env="
    },
    {
      "name": "ci",
      "depends": [
        "build",
        "test",
        "lint",
        "fmt"
      ],
      "dir": "."
    },
    {
      "name": "fmt",
      "cmd": "cargo fmt --version >/dev/null 2>&1 || rustup component add rustfmt; cargo fmt --all -- --check",
      "dir": ".",
      "leaf_identity": "cargo fmt --version >/dev/null 2>&1 || rustup component add rustfmt; cargo fmt --all -- --check|dir=.|env="
    },
    {
      "name": "lint",
      "cmd": "cargo clippy --version >/dev/null 2>&1 || rustup component add clippy; cargo clippy --workspace --all-targets --locked -- -D warnings",
      "dir": ".",
      "leaf_identity": "cargo clippy --version >/dev/null 2>&1 || rustup component add clippy; cargo clippy --workspace --all-targets --locked -- -D warnings|dir=.|env="
    },
    {
      "name": "test",
      "cmd": "cargo nextest run --workspace --all-targets --profile ci --no-tests=fail --color=always",
      "dir": ".",
      "leaf_identity": "cargo nextest run --workspace --all-targets --profile ci --no-tests=fail --color=always|dir=.|env="
    }
  ],
  "reachable_leaves": [
    {
      "identity": "cargo build --workspace --all-targets --locked|dir=.|env=",
      "tasks": [
        "build"
      ]
    },
    {
      "identity": "cargo clippy --version >/dev/null 2>&1 || rustup component add clippy; cargo clippy --workspace --all-targets --locked -- -D warnings|dir=.|env=",
      "tasks": [
        "lint"
      ]
    },
    {
      "identity": "cargo fmt --version >/dev/null 2>&1 || rustup component add rustfmt; cargo fmt --all -- --check|dir=.|env=",
      "tasks": [
        "fmt"
      ]
    },
    {
      "identity": "cargo nextest run --workspace --all-targets --profile ci --no-tests=fail --color=always|dir=.|env=",
      "tasks": [
        "test"
      ]
    },
    {
      "identity": "mise install --locked|dir=.|env=",
      "tasks": [
        "gate:install"
      ]
    }
  ],
  "findings": []
}
